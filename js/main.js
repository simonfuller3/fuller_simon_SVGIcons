(() => {
	// self invoking anonymous function or lambda
	var images = document.querySelectorAll('img');
	// var => means create a variable
	// whatever comes after the eqauls sign is what the variable 
	// 
	// document is always the webpage
	// after the dot is always a method or a prperty
		console.log(images);

